#include <stdio.h>
#include "CheckBit.h"

int main()
{
   BitType bit_type = getBitType();

   if (bit_type == 64_BIT) {
     printf("Running on 64 Bit");
   } else it (bit_type == 32_BIT) {
     printf("Running on 64 Bit");
   }
}
